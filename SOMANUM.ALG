Algoritmo "somanumeroslidos"

Var
num, soma: inteiro

Inicio

num <- 0
soma <- 0

enquanto (num <= 12) faca
  escreva("Digite um número inteiro:")
  leia(num)
  soma <- soma + num
  num <- num + 1
fimenquanto

escreva("A soma dos números lidos é:", soma)

Fimalgoritmo